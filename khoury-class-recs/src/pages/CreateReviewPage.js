export default function CreateReviewPage() {
  /**
   * select from drop down which class you want to review
   * - should only include classes you've taken + classes you haven't already reviewed
   * - once selected, form appears
   * - if you select a different option for the drop down, the form should clear all the
   *   input fields and update the form to say the new selected class
   */
  /**
   * Form
   * - Header: "Review CSXXXX"
   * - Difficulty: MUI NumberInput
   * - Workload: MUI NumberInput
   * - Overall Rating: MUI Rating
   * - Additional Comments (optional): MUI Textarea
   */
}
